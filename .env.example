# Backend API server origin (no trailing slash, no /api/v1).
# Production: https://api-server-i1.mytravellerschoice.com
VITE_API_URL=

# This frontend's public origin — used for payment-gateway return URLs.
VITE_CLIENT_URL=http://localhost:3000

# Optional
VITE_GOOGLE_MAPS_API_KEY=
